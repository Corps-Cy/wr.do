# ===========================================
# wr.do 生产环境配置模板
# ===========================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填写下面的配置信息
# 3. 确保 .env 文件不会被提交到 git（已在 .gitignore 中）
# ===========================================

# ===========================================
# 基础配置 (必需)
# ===========================================
# 应用访问地址（用于 OAuth 回调等）
NEXTAUTH_URL=https://your-domain.com
AUTH_URL=https://your-domain.com

# 认证密钥（必需，使用 openssl rand -base64 32 生成）
AUTH_SECRET=your-super-secret-auth-secret-here-generate-with-openssl-rand-base64-32

# 数据库连接（必需）
# PostgreSQL 格式: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/wrdo_db

# ===========================================
# 客户端配置 (必需)
# ===========================================
# 应用公网访问地址
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@your-domain.com
NEXT_PUBLIC_APP_NAME=wr.do

# ===========================================
# OAuth 登录配置 (可选)
# ===========================================
# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# LinuxDo OAuth
LinuxDo_CLIENT_ID=your-linuxdo-client-id
LinuxDo_CLIENT_SECRET=your-linuxdo-client-secret

# ===========================================
# 邮件服务配置 (可选)
# ===========================================
# Resend 邮件服务
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL=noreply@your-domain.com

# 其他邮件服务（可选）
FORWARD_EMAIL_API_KEY=your-forward-email-api-key
IMPROVMX_API_KEY=your-improvmx-api-key
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain

# ===========================================
# 阿里云 DNS 配置 (可选)
# ===========================================
ALIYUN_ACCESS_KEY_ID=your-aliyun-access-key-id
ALIYUN_ACCESS_KEY_SECRET=your-aliyun-access-key-secret
ALIYUN_REGION=cn-hangzhou

# ===========================================
# 其他服务配置 (可选)
# ===========================================
# ScreenshotOne API
SCREENSHOTONE_BASE_URL=your-screenshotone-base-url

# GitHub Token（用于某些 GitHub API 调用）
GITHUB_TOKEN=your-github-token

# 邮件附件存储域名
NEXT_PUBLIC_EMAIL_R2_DOMAIN=your-email-attachment-domain

# ===========================================
# Docker 部署配置 (可选)
# ===========================================
# 端口号（默认 3000，与 docker-compose.prod.yml 中的端口映射对应）
PORT=3000

# 跳过数据库检查（生产环境建议设为 false）
SKIP_DB_CHECK=false

# 跳过数据库迁移（生产环境建议设为 false，让容器自动运行迁移）
SKIP_DB_MIGRATION=false
